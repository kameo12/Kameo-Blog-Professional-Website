//Javascript function: changes opacity of headshot image when click on, must refresh page to back to original opacity.
$('.image').on('click',function(){
//changes image opacity when the headshot image is clicked on
$('.image') .fadeTo ("slow", 0.65);
});
